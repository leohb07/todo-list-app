import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    gap: 12,
    backgroundColor: "#262626",
    padding: 12,
    alignItems: "center",
    borderRadius: 8,
    marginTop: 8,
  },
});
